class City:
    def __init__(self, id, name, country_id, population, airports, climat):
        self.id = int(id)
        self.name = name
        self.country_id = int(country_id)
        self.population = int(population)
        self.airports = int(airports)
        self.climat = climat